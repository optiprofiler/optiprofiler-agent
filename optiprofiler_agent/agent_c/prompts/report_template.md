# Benchmark Analysis Report

## Overview

Summarize the experiment setup (solvers, problem types, dimension range, feature, number of problems, wall-clock time) and the overall solver scores/rankings.

## Performance Profile Analysis

Analyze the performance profiles across tolerance levels:
- Which solver is more efficient (higher at τ=1)?
- Which solver is more robust (higher as τ→∞)?
- How do rankings change as tolerance tightens?
- Note any precision cliffs (sharp score drops between tolerances).

## Data Profile Analysis

Analyze the data profiles:
- Which solver uses fewer function evaluations?
- How does evaluation efficiency compare across tolerances?

## Convergence Issues

Summarize convergence failure patterns:
- Which problems consistently fail across all solvers?
- Are failures concentrated at tight tolerances or spread across all?
- List the most problematic test problems.

## Anomalies and Warnings

Report any detected anomalies:
- Extreme function values
- Timing outliers
- Solver plateaus at specific tolerances
- Significant solver divergence at tight tolerances

## Recommendations

Based on the data, provide actionable advice:
- Which solver to prefer for which use case (efficiency vs. robustness)?
- Which tolerance range is most informative?
- Any caveats about the experiment setup?

---

*Report auto-generated by OptiProfiler Agent*
